[pytest]
DJANGO_SETTINGS_MODULE = NexusAPI.settings
python_files = tests.py test_*.py *_tests.py
addopts =  --reuse-db --ds=NexusAPI.settings -m "not benchmark"
markers =
    benchmark: endpoint performance audit; excluded from normal runs, run with -m benchmark
